[project]
name = "rag-against-the-machine"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fire",
    "pydantic",
    "tqdm",
    "langchain-text-splitters",
    "bm25>=0.3.11",
    "torch>=2.13.0",
    "transformers>=5.16.1",
    "accelerate>=1.14.0",
    "bm25s>=0.3.11",
    "pystemmer>=3.1.0",
]

[dependency-groups]
dev = [
    "flake8",
    "mypy",
]
