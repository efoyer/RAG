[project]
name = "rag-against-the-machine"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fire",
    "pydantic",
    "tqdm",
    "langchain-text-splitters",
    "bm25>=0.3.11",
]

[tool.uv]
dev-dependencies = [
    "flake8",
    "mypy",
]
